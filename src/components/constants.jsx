const DNS = 'http://localhost:3001';
const REGISTER_URL = `${DNS}/api/1.0/user/signup`;
const LOGIN_URL = `${DNS}/api/1.0/user/signin`;

const constants = { REGISTER_URL, LOGIN_URL };

export default constants;
